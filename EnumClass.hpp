#pragma once

enum class MenuState {IDLE, MENU, BULLETS, LIVES, PLAYER_COUNT, PLAY_TIME};
enum class GunState {IDLE, SHOOT, RELOAD};
enum class GameState{IDLE, SETTINGS, COUNTDOWN, GAME, FINISH};
enum class DisplayState {IDLE, SETTINGS, GAME, FINISH};
