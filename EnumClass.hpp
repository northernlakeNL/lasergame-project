#pragma once

enum class GunState {IDLE, SHOOT, RELOAD};
enum class GameState{IDLE, SETTINGS, GAME, SEND, FINISH};
enum class DisplayState {SETTINGS, GAME, FINISH};
enum class MenuState {IDLE, MENU, BULLETS, LIVES, PLAYER_COUNT, PLAY_TIME};
enum class DisplayMenuState {IDLE, MENU, BULLETS, LIVES, PLAYER_COUNT, PLAY_TIME, DONE, ERROR, GAME, FINISH};
